import { Meteor } from 'meteor/meteor';
import '../imports/api/attachments/methods';
import '../imports/api/attachments/http_upload';

Meteor.startup(() => {
  // code to run on server at startup
});
